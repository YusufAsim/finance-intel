ML_LOG_LEVEL=INFO
# upstream backend used to pull training data
BACKEND_BASE_URL=http://backend:8000
BACKEND_TIMEOUT_SECONDS=10
# trained model files live here, backed by a named volume
ARTIFACTS_DIR=/artifacts
